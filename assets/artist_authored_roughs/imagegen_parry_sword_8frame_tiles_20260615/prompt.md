# Parry Sword 8-Frame Rough Source

Generated with the built-in image generation path as two 2x2 rough source sheets.

Target action:

- `parry_sword`
- side-view right-facing
- one-shot
- 8 source frames
- parry frames: `3`, `4`

Phase groups:

- sheet 00: `ready`, `raise_guard`, `brace`, `parry_contact`
- sheet 01: `deflect`, `recoil_hold`, `recover`, `ready_return`

Prompt constraints used:

- one character only;
- pink bob hair;
- sailor-style white top;
- red tie;
- navy skirt;
- dark socks;
- brown loafers;
- simple one-handed short sword;
- clear defensive guard, contact, deflect, and recovery;
- flat `#00ff00` chroma-key background;
- no enemy character, frame labels, text, watermarks, duplicate bodies, afterimages, crowds, or
  background shadows.

Review note:

This rough source is accepted as a first-pass sword parry. The parry contact frames are readable
and the sword remains connected to the hand. The green hit sparkle is source art, not runtime
metadata; gameplay timing is recorded separately as `parry_frames: [3, 4]`.
