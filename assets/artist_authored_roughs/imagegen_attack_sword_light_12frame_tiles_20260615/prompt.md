# Attack Sword Light 12-Frame Rough Source

Generated with the built-in image generation path as three 2x2 rough source sheets.

Target action:

- `attack_sword_light`
- side-view right-facing
- one-shot
- 12 source frames
- active hit frames: `5`, `6`

Phase groups:

- sheet 00: `ready`, `anticipation`, `draw_back`, `windup`
- sheet 01: `slash_start`, `active_slash`, `active_follow_through`, `overshoot`
- sheet 02: `recoil`, `settle`, `recover`, `ready_return`

Prompt constraints used:

- one character only;
- pink bob hair;
- sailor-style white top;
- red tie;
- navy skirt;
- dark socks;
- brown loafers;
- simple one-handed short sword;
- consistent side-view right-facing full-body pose;
- flat `#00ff00` chroma-key background;
- no frame labels, text, watermarks, duplicate bodies, crowds, or background shadows.

Review note:

This rough source is an accepted first-pass light sword attack source, not a general attack solution.
The active frames are readable enough for the current character sprite asset pack, but future weapon
actions should still use action-specific rough sheets and explicit hit-frame metadata.
